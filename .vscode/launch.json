{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug (clang) - Active File",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/output", // Имя выходного файла из tasks.json
      "args": [],
      "stopAtEntry": true,
      "cwd": "${workspaceFolder}",
      "environment": [],
      "externalConsole": true,
      "MIMode": "lldb",
      "preLaunchTask": "C/C++: clang build active file"
    }
  ]
}
